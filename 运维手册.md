# 韩轩一品装饰 · 官网运维手册

> 写给非技术人员 / 刚接手运维的同事。所有改动只需要记事本级别能力即可完成。

---

## 一、项目结构速览

```
website/
├── index.html          首页（Hero + 数据 + 业务 + 案例 + CTA）
├── about.html          关于我们（简介 + 时间线 + 价值观 + 资质）
├── services.html       产品服务（5 大业务 + 合作流程）
├── cases.html          客户案例（12 个项目 + 证言 + 筛选）
├── team.html           团队介绍（管理层 + 设计师 + 施工监理）
├── news.html           新闻资讯（12 篇文章 + 分类 + 分页）
├── contact.html        联系我们（信息 + 表单 + 地图 + FAQ）
├── css/style.css       统一样式（颜色 / 布局 / 响应式 / 动画）
├── js/main.js          交互逻辑（菜单 / 滚动动画 / 案例筛选 / 表单）
└── assets/images/      所有图片都放在这里（目前是空的，渐变占位）
```

**核心原则**：文字直接写在 HTML 里，图片路径在 HTML 里引用，CSS/JS 共用一套。

---

## 二、四种运维方案对比

| 方案 | 难度 | 适合场景 | 工作量 |
|---|---|---|---|
| **① 直接编辑 HTML**（推荐） | ⭐ | 月改 1–10 次，公司自有运维 | 改一处 5 分钟 |
| ② 静态站点生成器（Hugo/11ty） | ⭐⭐⭐ | 频繁更新博客/资讯，需要版本控制 | 需学习 Markdown + 命令行 |
| ③ 无头 CMS（Strapi/腾讯轻联） | ⭐⭐⭐⭐ | 非技术人员运营，图文编辑后台 | 一次性配置后台 |
| ④ 完整动态站（Next.js + 数据库） | ⭐⭐⭐⭐⭐ | 大流量、多角色、需要登录 | 全栈开发 |

**首推方案 ①**：维护成本最低，能解决 80% 的场景。后面单独写进阶章节讲 ②③④。

---

## 三、方案①：直接编辑 HTML（推荐）

### 你需要准备的工具

| 工具 | 推荐 | 作用 |
|---|---|---|
| 代码编辑器 | **VSCode**（免费） | 高亮语法、智能提示、文件树 |
| 浏览器 | Chrome / Edge | 查看效果 |
| 图片处理 | 画图 3D / 在线压缩工具 tinypng.com | 缩小图片体积 |
| FTP 客户端 | FileZilla（仅部署用） | 上传到服务器 |

VSCode 下载：https://code.visualstudio.com/

### 场景 A · 替换文字（电话 / 地址 / 文案）

**Step 1** · 打开 VSCode → 文件 → 打开文件夹 → 选择 `website/`

**Step 2** · 在左侧文件树找到要改的页面（举例：`contact.html`）

**Step 3** · 按 `Ctrl + Shift + F` 全局搜索（搜索整个文件夹）
- 输入要改的内容，比如：`0592-0000000`
- VSCode 会列出所有出现位置，双击跳转到对应文件

**Step 4** · 双击替换为真实内容，按 `Ctrl + S` 保存

**Step 5** · 浏览器按 `F5` 刷新查看效果

> ⚠️ 同一信息可能在多个文件出现，比如电话号码会同时出现在 contact.html / index.html / services.html，要全部改完。

### 场景 B · 替换图片（Hero / 案例 / 团队头像）

**Step 1** · 准备图片
- 案例图：1200×800 像素，JPG 或 WebP，单张 ≤ 500KB
- 团队头像：400×400 像素，正方形
- 用 [tinypng.com](https://tinypng.com/) 压缩到 ≤ 500KB

**Step 2** · 命名规范（用英文小写 + 数字）
- ✅ `case-living-01.jpg` / `team-zhang.jpg`
- ❌ `微信图片_20260315.jpg`（中文、空格会被浏览器拒绝）

**Step 3** · 把图片复制到 `website/assets/images/`

**Step 4** · 打开对应的 HTML 文件
- 案例图改 `cases.html` 和 `index.html`
- 团队头像改 `team.html`

**Step 5** · 找到 `.case-card-image` 这类 CSS 类（用 `Ctrl + F` 搜索）
- 把里面的 `background: linear-gradient(...)` 改为：
  ```css
  background-image: url('assets/images/case-living-01.jpg');
  background-size: cover;
  background-position: center;
  ```

**Step 6** · `Ctrl + S` 保存 → 浏览器 `Ctrl + F5` 强制刷新（绕过缓存）

---

## 四、常用内容速查表

打开 VSCode，按 `Ctrl + Shift + F` 全局搜索这些占位文字，一键定位：

| 占位文字 | 实际含义 | 出现在哪些文件 |
|---|---|---|
| `0592-0000000` | 客服热线 | contact.html / index.html / services.html |
| `138-0000-0000` | 项目咨询电话 | contact.html |
| `123@hanxuan.com` | 联系邮箱 | contact.html |
| `思明区软件园二期观日路 56 号 12 楼` | 公司地址 | contact.html |
| `公司 © 2026 厦门韩轩一品装饰设计工程有限公司` | 版权 | 所有页面底部 |
| `闽ICP备XXXXXXXX号` | ICP 备案号 | contact.html 底部 |
| `+86 0000 0000` | 备用电话 | team.html |
| `800+` `98%` `12+` `36+` | 数据指标 | index.html Hero 区 |
| `12 / 8 / 36 / 800 / 98` | 数字硬编码 | 全部页面的统计区 |

> 💡 一次全部替换：搜 `0592-0000000` → `Ctrl + H`（替换模式）→ 输入新号码 → 点"全部替换"。

---

## 五、改完本地后：上线到公网

### 方案 A · 国内正式部署（推荐用于正式营业）

需要：注册域名 + 备案 + 静态托管

| 服务商 | 价格 | 操作 |
|---|---|---|
| **阿里云 OSS** | ¥0.08/GB/月 | 开通 OSS → 上传 website/ 全部文件 → 开启静态网站托管 |
| **腾讯云 COS** | ¥0.1/GB/月 | 同上 |
| **传统虚拟主机** | ¥100–300/年 | FileZilla 拖拽上传 |

### 方案 B · 国外免备案（快速试运行）

| 平台 | 价格 | 操作 |
|---|---|---|
| **Netlify** | 免费 | 把 website/ 文件夹拖到 netlify.com → 自动分配域名 |
| **Vercel** | 免费 | 同上 |
| **GitHub Pages** | 免费 | 上传到 GitHub → 仓库 Settings → Pages → 选 main 分支 |

### 方案 C · 我帮您部署

告诉我您的服务器账号（OSS、COS、虚拟主机 FTP），我直接帮您传上去 + 配置域名。

---

## 六、进阶：未来想要更便捷？

### 痛点 1 · 我不会写代码，但想频繁改文案

**解法**：把内容抽到 `content.json`，让非技术人员只改 JSON 文件。我可以帮您改造。

### 痛点 2 · 公司多人协作，老是覆盖别人的改动

**解法**：用 Git 版本控制，VSCode 自带 Git 图形界面，无需命令。我可以帮您初始化。

### 痛点 3 · 想加一个"在线留言"功能，让客户能提交表单

**解法**：表单目前已对接腾讯文档收集表（端点预留）。配置步骤见下方"第九节：腾讯文档表单接收地址配置"。

### 痛点 4 · 想加一个新闻发布后台

**解法**：用 **Strapi / Directus** 这类无头 CMS，或直接用 **腾讯文档 + Notion** 当内容源，每周一键同步。我可以帮您接入。

---

## 七、日常运维安全清单

每次改完上线前，花 2 分钟检查：

- [ ] 浏览器看不到占位文字 `0592-0000000`
- [ ] 所有图片能正常加载（无破图）
- [ ] 移动端用 F12 模拟器看一眼没乱版
- [ ] 提交表单试一次，看是否正常提示
- [ ] 改了文件 → 备份了原文件（防止回滚）

---

## 八、常见问题 FAQ

**Q1：改错文件怎么恢复？**
A：VSCode 文件 → 时间线 → 选中之前的版本右键"恢复内容"。

**Q2：图片上传后浏览器没变化？**
A：`Ctrl + F5` 强制刷新（绕过浏览器缓存）。如果还不行，检查 HTML 里图片路径是否和文件名大小写一致（Linux 服务器区分大小写）。

**Q3：我想改主题色怎么办？**
A：打开 `css/style.css` 第 8-20 行附近的 `:root` 变量，改 `--color-primary` 一个值，全站自动变。

**Q4：网站打开很慢怎么办？**
A：99% 是图片没压缩。检查每张图是否 ≤ 500KB。

**Q5：怎么加新页面？**
A：复制任意 `about.html` 整段 → 改名 `new-page.html` → 修改导航（在所有页面的 `<nav>` 里加链接）。

---

## 九、腾讯文档表单接收地址配置

> 客户在「联系我们」页面填写的咨询信息，最终将提交到您指定的腾讯文档收集表。本节说明如何填入真实端点。

### 9.1 当前状态

代码已经预留好接收地址常量，文件位置：`website/js/main.js` 第 18 行：

```js
const TENCENT_DOCS_FORM_URL = 'PENDING_TENCENT_DOCS_URL';
```

`PENDING_TENCENT_DOCS_URL` 是占位符。当它是占位符时：
- ✅ 表单仍能正常提示"✓ 已收到，我们会尽快与您联系"
- ❌ **客户填写的姓名、电话、需求不会被保存到任何地方**

### 9.2 后续拿到腾讯文档地址后，按以下步骤配置

**Step 1** · 打开 `website/js/main.js`

**Step 2** · 用 `Ctrl + F` 搜索 `PENDING_TENCENT_DOCS_URL`，会找到 2 处：
- 第 18 行：常量定义（要改）
- 第 19 行：占位符常量（不要改）

**Step 3** · 把第 18 行的 `'PENDING_TENCENT_DOCS_URL'` 替换为腾讯文档给您的真实 URL，例如：

```js
const TENCENT_DOCS_FORM_URL = 'https://docs.qq.com/form/fill/您的收集表ID';
```

**Step 4** · `Ctrl + S` 保存 → 浏览器 `Ctrl + F5` 强制刷新

**Step 5** · 自己填写一次表单测试 → 去腾讯文档后台确认是否收到

### 9.3 如何从腾讯文档拿到提交端点

| 收集表来源 | 获取方式 |
|---|---|
| 您自己创建的腾讯文档收集表 | 收集表 → 右上角「分享」→ 复制"填写链接"，通常就是提交端点 |
| 腾讯文档 API 控制台 | 腾讯文档开放平台 → 我的应用 → 表单 API → 拿到 webhook URL |
| 通过腾讯文档与 webhook 集成 | 收集表设置 → 推送通知 → 启用 webhook 拿到 URL |

> 💡 **如果您拿到的是"填写链接"**（客户打开表单用的 GET 链接），请把链接发给我，我帮您判断是否可直接作为 POST 端点用，或者需要做一个简单的转发服务。

### 9.4 字段对应关系

表单字段会自动以 FormData 形式 POST 出去，字段名与 HTML 一致：

| 前端字段名（HTML） | 中文含义 |
|---|---|
| `name` | 客户姓名 |
| `phone` | 联系电话 |
| `email` | 邮箱 |
| `service` | 咨询的业务类型 |
| `area` | 装修面积 |
| `budget` | 预算范围 |
| `message` | 留言详情 |

请确保您的腾讯文档收集表的字段名与此一致，否则数据无法对应。如需调整字段名，直接改 HTML 中的 `name="..."` 即可。

### 9.5 测试清单

配置完成后，**必须做一次完整测试**：

- [ ] 自己填写一次表单提交
- [ ] 去腾讯文档后台确认收到 1 条新数据
- [ ] 检查每个字段是否完整对应
- [ ] 用手机扫码访问页面，再测一次
- [ ] 失败时浏览器控制台（F12）会显示 `[表单] 提交到腾讯文档失败` 错误

---

## 十、获取帮助

- 📖 本手册即查即用
- 💬 任何问题随时问我，告诉我："运维手册第 X 节那个问题"
- 🔧 复杂改动（接后端 / 换主题 / 加新页面）告诉我需求，我直接帮您改

---

*文档最后更新：2026-09-15*
